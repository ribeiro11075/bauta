"""What the generating commands build from a live schema: a masking policy
(`discover`), a referentially complete subset (`subset`), the copy's tables
(`schema`) and rows for tables that can't be copied (`synthesize`).
"""
