# Every package `bauta[all]` installs, pinned to one tested set.
# CI's integration job runs against these. After changing a version,
# regenerate the rest with:
#
#   uv pip compile pyproject.toml --extra all -c <this file's direct pins> \
#       --python-version 3.14 --python-platform linux --no-header --no-annotate
#
# tests/repository/test_packaging.py checks every pin here is within pyproject.toml's ranges.
annotated-types==0.8.0
cffi==2.1.1
cryptography==50.0.1
duckdb==1.5.5
mysql-connector-python==26.7.0
oracledb==26.0.0
psycopg==3.3.6
psycopg-binary==3.3.6
pyarrow==25.0.1
pycparser==3.0
pydantic==2.13.5
pydantic-core==2.46.5
pymssql==2.4.1
pytz==2026.4
pyyaml==6.0.3
typing-extensions==4.16.0
typing-inspection==0.4.4
